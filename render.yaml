services:
  - type: web
    name: ai-insiders-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_improved.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_IDS
        sync: false
    autoDeploy: true
